import java.nio.file.Files;
import java.nio.file.Path;
import java.util.List;

public class PrintDemoted {
    public static void main(String[] args) throws Exception {
        // Just parsing the output of the evaluator wasn't saved, I will re-run the same script but print demoted
    }
}
